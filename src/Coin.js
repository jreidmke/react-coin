import {React, useState} from 'react';

const Coin = () => {
    //set coin state using useState
    //default coin to 'H' for heads
    //const [rand, setRand] = useState(0);
    //const flip = () => setRand(Math.random());
    //const rand = Math.random();
    //const heads = rand > .5;

    /* return (
        <div>
            <h3>{heads ? 'H' : 'T'}<h3>
            <button>
        </div>
    )*/
}